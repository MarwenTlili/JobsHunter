{% extends 'layout.html.twig' %}

{% block title %}Edit PprofessionalInterest{% endblock %}

{% block body %}
    <h1>Edit ProfessionalInterest</h1>

    <div class="container-fluid">
        <div class="row">
            <div class="col-4">
                {{ include('cv/_sidebar.html.twig') }}
            </div>
            <div class="col-8">
                {{ include('professional_interest/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>

{% endblock %}
