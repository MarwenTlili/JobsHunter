{% extends 'layout.html.twig' %}

{% block title %}New GeneralInformation{% endblock %}

{% block body %}
    <h1>Create new GeneralInformation</h1>

    <div class="container-fluid">
        <div class="row">
            <div class="col-4">
                {{ include('cv/_sidebar.html.twig') }}
            </div>
            <div class="col-8">
                {# <a href="{{ asset('uploads/brochures/' ~ form.photo) }}">View brochure (PDF)</a> #}

                {{ include('general_information/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
